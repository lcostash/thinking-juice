<img src="{{ Image(imgID).src(imgSize ~ '-desktop') }}" alt="{{ imgAlt }}" class="{{ imgClass }}"
     data-srcset="{{ Image(imgID).src(imgSize ~ '-mobile') }} 360w,
                {{ Image(imgID).src(imgSize ~ '-tablet') }} 768w,
                {{ Image(imgID).src(imgSize ~ '-desktop') }} 1440w,
                {{ Image(imgID).src(imgSize ~ '-jumbo') }} 2000w" />